<?xml version="1.0" encoding="UTF-8"?>
<statements namespace="org.lsqt.syswin.authority.model.PositionPermitResult" cache="true" import="org.lsqt.components.util.collection.ArrayUtil,org.lsqt.components.util.lang.StringUtil" >
	<table name="t_power_duties_permit_result">
			<column id="id" type="auto" property="id" text="主键" />
		
	 	
	 		<column name="duties_id" property="positionId" text="岗位id"/>
	 	
	 		<column name="module_code" property="moduleCode" text="功能模块编码"/>
	 	
	 		<column name="org_id" property="orgId" text="数据ID"/>
	 	
	 		<column name="type" property="type" text="权限类型：1=数据查询 2=数据使用"/>
	 	
			<column name="level" property="level" text="授权范置：1=本人 2=下属下级 3=所有下级"/>
		
	 		
	 		
	 		
	 		
	 		
	 		
	 		
	 		
	 		
	 		
	</table>
	
	<statement id="queryForPage" parameterName="query">
	 	<![CDATA[
		 	select * from t_power_duties_permit_result where 1=1
		 	<#if query??>
		 		<#-- -------------------关键字查询开始 --------------- -->
				<#if query.key??>
					<#assign key = StringUtil.escapeSql(query.key)/>
					and (
							 					
							 
							module_code like '%${key}%' 
							
					)
				</#if>
				<#-- -------------------关键字查询结束 --------------- -->
	
				<#-- -------------------综合查询开始 --------------- -->
				
				<#if query.ids??> and id in (${StringUtil.escapeSql(query.ids)}) </#if>
						
	  					<#if query.id??> and id = ${query.id} </#if>
					
						
	  					<#if query.positionId??> and duties_id = ${query.positionId} </#if>
					
	  						<#if query.moduleCode??> and module_code = '${StringUtil.escapeSql(query.moduleCode)}' </#if>
						
					
						
	  					<#if query.orgId??> and org_id = ${query.orgId} </#if>
					
						
	  					<#if query.type??> and type = ${query.type} </#if>
					
				<#-- -------------------综合查询结束 --------------- -->
        	</#if>
        	
		]]>
	</statement>
	
	<statement id="getAll">
		<![CDATA[ select * from t_power_duties_permit_result ]]>
	</statement>
	
</statements>
